import React from "react";
import { StyleSheet, Text, View } from "react-native";
import UserScreenContent from "../src/components/UserScreenContent";

export default function UserScreen() {
  return (
    <View>
      <UserScreenContent />
    </View>
  );
}

const styles = StyleSheet.create({});
